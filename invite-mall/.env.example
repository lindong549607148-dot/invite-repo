PORT=3000
AUTO_RECEIVE_DAYS=10
PAYOUT_DELAY_DAYS=3
DAILY_BASE_QUOTA=1
JWT_SECRET=your-jwt-secret-change-in-prod
ADMIN_KEY=your-admin-key-change-in-prod
DAILY_BONUS_MAX=1

# 任务与灰度开关
REQUIRED_HELPERS=2
DAILY_START_QUOTA=1
ALLOW_DUP_HELPER=0
AMOUNT_CHECK_ENABLED=0
RISK_ENABLED=1
ENABLE_ORDER_EXPIRE_CLOSE=1
ENABLE_PAYOUT_LEDGER=1
ENABLE_RISK_BLOCKING=0
PAYOUT_DELAY_RISK_MEDIUM_DAYS=3
ENABLE_TASK_PROGRESS_API=1
ENABLE_LOGISTICS_API=1

# 调度器：自动收货 + 到期审核扫描
SCHED_ENABLED=1
SCHED_INTERVAL_SEC=30

# 订单超时关单（分钟）
ORDER_PAY_EXPIRE_MINUTES=30

# 风控第一层（灰度 + 各规则开关）
RISK_GLOBAL_PERCENT=100

RISK_IP_LIMIT_ENABLED=false
RISK_IP_MODE=log
RISK_IP_HELPER_MAX_PER_DAY=3

RISK_DEVICE_LIMIT_ENABLED=false
RISK_DEVICE_MODE=review
RISK_DEVICE_HELPER_MAX_PER_DAY=2

RISK_ADDRESS_LIMIT_ENABLED=false
RISK_ADDRESS_MODE=review
RISK_ADDRESS_MAX_PER_DAY=2

RISK_GRAPH_ENABLED=false
RISK_GRAPH_MODE=log
RISK_GRAPH_HELPER_MAX_INVITERS_PER_DAY=1
